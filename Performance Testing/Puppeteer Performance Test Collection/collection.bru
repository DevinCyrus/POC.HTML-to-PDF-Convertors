docs {
  This collection is designed specifically for performance testing, the requests are all batch duplicates of the same call for different reports.
  
  Utilise the Bruno runner to run all of the requests in this collection sequentially with a single click, repeat runs until desired sample size reached for performance testing.
  
  Each requests time to generate and generated file size are captured by the performance logger.
  
  "..\PdfPerfLogs\perf-results.csv"
}
